source "https://rubygems.org"

# Use the GitHub Pages gem to ensure compatibility with GitHub's environment
gem "github-pages", "~> 226", group: :jekyll_plugins
gem "jekyll-include-cache"

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
end
